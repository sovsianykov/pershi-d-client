import React from 'react';
import styles from "./Home.module.scss"

const Home = () => {
    return (
        <main className={styles.home}>
           home
        </main>
    );
};

export default Home;